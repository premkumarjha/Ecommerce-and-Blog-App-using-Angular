import { PracticeDirective } from './practice.directive';

describe('PracticeDirective', () => {
  it('should create an instance', () => {
    const directive = new PracticeDirective();
    expect(directive).toBeTruthy();
  });
});
